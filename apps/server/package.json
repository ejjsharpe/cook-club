{
  "name": "cook-club-server",
  "scripts": {
    "dev": "wait-on tcp:8788 && wrangler dev --ip 0.0.0.0",
    "deploy": "wrangler deploy --minify"
  },
  "dependencies": {
    "@better-auth/expo": "^1.4.1",
    "@hono/trpc-server": "^0.4.0",
    "better-auth": "^1.4.1",
    "hono": "^4.10.6"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250718.0",
    "@types/node": "^24.0.14",
    "eslint-config-universe": "15.0.3",
    "typescript": "^5.8.3",
    "wait-on": "^9.0.3",
    "wrangler": "^4.50.0"
  },
  "eslintConfig": {
    "extends": "universe/node"
  }
}
