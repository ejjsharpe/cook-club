{
  "name": "cook-club",
  "devDependencies": {
    "turbo": "^2.4.2"
  },
  "packageManager": "bun@1.2.3",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "check-types": "turbo run check-types"
  },
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "trustedDependencies": [
    "esbuild",
    "sharp",
    "workerd"
  ]
}
